[Unit]
Description=CmdBar CLI-Only Headless Server Mode
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 -m companion.server --host 127.0.0.1 --port 8080
Restart=always
RestartSec=5s
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=default.target
